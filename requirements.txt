# Agent 端 (Python 3.11+)。機器人端的 robot_server.py 另外在 rmenv 裡跑，只需要 robomaster 和 opencv-python。
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0.0
psutil>=5.9
httpx>=0.27
opencv-python>=4.9
numpy>=1.26
pillow>=10.0

# 依照你用的 provider 擇一安裝即可
openai>=1.40.0
anthropic>=0.40.0
google-genai>=1.0.0
ollama>=0.4.0

# RobotAgentBU.py 用真的 browser_use.Agent (見 browser_use/ 底下 vendor 進來的完整套件)，
# 這些是它 import 時真正會用到、缺了就會直接 ImportError 的第三方套件。
cdp-use>=1.4.0
bubus>=1.5.0
uuid7>=0.1.0            # 提供的模組名稱其實是 uuid_extensions，不是 uuid7
posthog>=3.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
groq>=0.9.0
ollama>=0.4.0
InquirerPy>=0.3.4
rich>=13.0.0
reportlab>=4.0.0
tiktoken>=0.7.0
pyperclip>=1.8.0
pyotp>=2.9.0
cloudpickle>=3.0.0
click>=8.1.0
